{
  "version": 2,
  "buildCommand": "npm ci && npm run build",
  "builds": [
    { "src": "src/serverless.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/serverless.ts" }
  ]
}
